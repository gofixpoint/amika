#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

if [ ! -f "$REPO_ROOT/dist/amika" ]; then
  echo "Building amika..." >&2
  (cd "$REPO_ROOT" && go build -o dist/amika ./cmd/amika)
fi

exec "$REPO_ROOT/dist/amika" "$@"
