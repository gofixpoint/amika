#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

if [ ! -f "$REPO_ROOT/dist/amikactl" ]; then
  echo "Building amikactl..." >&2
  (cd "$REPO_ROOT" && go build -o dist/amikactl ./cmd/amikactl)
fi

exec "$REPO_ROOT/dist/amikactl" "$@"
